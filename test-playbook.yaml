---
- name: Ping remote host
  hosts: all
  gather_facts: false

  tasks:
    - name: Ping the remote host
      ansible.builtin.ping:
      register: ping_result

    - name: Show ping result
      ansible.builtin.debug:
        msg: "Ping response from {{ inventory_hostname }}: {{ ping_result.ping }}"
